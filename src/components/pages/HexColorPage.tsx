import SidebarApp from '../SidebarApp';
import HexColorContent from '../../app/(sidebar-layout)/hex-color-code/Content';

const HexColorPage = () => (
  <SidebarApp currentPath="/hex-color-code">
    <HexColorContent />
  </SidebarApp>
);

export default HexColorPage;
