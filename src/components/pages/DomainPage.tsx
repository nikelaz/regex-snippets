import SidebarApp from '../SidebarApp';
import DomainContent from '../../app/(sidebar-layout)/domain/Content';

const DomainPage = () => (
  <SidebarApp currentPath="/domain">
    <DomainContent />
  </SidebarApp>
);

export default DomainPage;
