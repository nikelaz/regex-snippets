import SidebarApp from '../SidebarApp';
import TextLengthContent from '../../app/(sidebar-layout)/text-length/Content';

const TextLengthPage = () => (
  <SidebarApp currentPath="/text-length">
    <TextLengthContent />
  </SidebarApp>
);

export default TextLengthPage;
