import SidebarApp from '../SidebarApp';
import WindowsPathContent from '../../app/(sidebar-layout)/windows-path/Content';

const WindowsPathPage = () => (
  <SidebarApp currentPath="/windows-path">
    <WindowsPathContent />
  </SidebarApp>
);

export default WindowsPathPage;
