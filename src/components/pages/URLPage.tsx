import SidebarApp from '../SidebarApp';
import URLContent from '../../app/(sidebar-layout)/url-and-path/Content';

const URLPage = () => (
  <SidebarApp currentPath="/url-and-path">
    <URLContent />
  </SidebarApp>
);

export default URLPage;
