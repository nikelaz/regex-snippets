import SidebarApp from '../SidebarApp';
import TimeContent from '../../app/(sidebar-layout)/time/Content';

const TimePage = () => (
  <SidebarApp currentPath="/time">
    <TimeContent />
  </SidebarApp>
);

export default TimePage;
