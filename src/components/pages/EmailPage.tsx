import SidebarApp from '../SidebarApp';
import EmailContent from '../../app/(sidebar-layout)/email/EmailContent';

const EmailPage = () => (
  <SidebarApp currentPath="/email">
    <EmailContent />
  </SidebarApp>
);

export default EmailPage;
