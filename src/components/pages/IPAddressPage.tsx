import SidebarApp from '../SidebarApp';
import IPAddressContent from '../../app/(sidebar-layout)/ip-address/Content';

const IPAddressPage = () => (
  <SidebarApp currentPath="/ip-address">
    <IPAddressContent />
  </SidebarApp>
);

export default IPAddressPage;
