import SidebarApp from '../SidebarApp';
import HTMLTagContent from '../../app/(sidebar-layout)/html-tag/Content';

const HTMLTagPage = () => (
  <SidebarApp currentPath="/html-tag">
    <HTMLTagContent />
  </SidebarApp>
);

export default HTMLTagPage;
