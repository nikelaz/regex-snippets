import SidebarApp from '../SidebarApp';
import PhoneContent from '../../app/(sidebar-layout)/phone/Content';

const PhonePage = () => (
  <SidebarApp currentPath="/phone">
    <PhoneContent />
  </SidebarApp>
);

export default PhonePage;
