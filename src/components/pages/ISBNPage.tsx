import SidebarApp from '../SidebarApp';
import ISBNContent from '../../app/(sidebar-layout)/isbn/Content';

const ISBNPage = () => (
  <SidebarApp currentPath="/isbn">
    <ISBNContent />
  </SidebarApp>
);

export default ISBNPage;
