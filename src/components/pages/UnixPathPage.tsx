import SidebarApp from '../SidebarApp';
import UnixPathContent from '../../app/(sidebar-layout)/unix-path/Content';

const UnixPathPage = () => (
  <SidebarApp currentPath="/unix-path">
    <UnixPathContent />
  </SidebarApp>
);

export default UnixPathPage;
