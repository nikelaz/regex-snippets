import SidebarApp from '../SidebarApp';
import NumberOfLinesContent from '../../app/(sidebar-layout)/number-of-lines/Content';

const NumberOfLinesPage = () => (
  <SidebarApp currentPath="/number-of-lines">
    <NumberOfLinesContent />
  </SidebarApp>
);

export default NumberOfLinesPage;
