import SidebarApp from '../SidebarApp';
import UsernameContent from '../../app/(sidebar-layout)/username/Content';

const UsernamePage = () => (
  <SidebarApp currentPath="/username">
    <UsernameContent />
  </SidebarApp>
);

export default UsernamePage;
