import SidebarApp from '../SidebarApp';
import PasswordContent from '../../app/(sidebar-layout)/password/Content';

const PasswordPage = () => (
  <SidebarApp currentPath="/password">
    <PasswordContent />
  </SidebarApp>
);

export default PasswordPage;
