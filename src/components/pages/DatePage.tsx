import SidebarApp from '../SidebarApp';
import DateContent from '../../app/(sidebar-layout)/date/Content';

const DatePage = () => (
  <SidebarApp currentPath="/date">
    <DateContent />
  </SidebarApp>
);

export default DatePage;
