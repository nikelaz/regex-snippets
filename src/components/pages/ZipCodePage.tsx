import SidebarApp from '../SidebarApp';
import ZipCodeContent from '../../app/(sidebar-layout)/zip-code/Content';

const ZipCodePage = () => (
  <SidebarApp currentPath="/zip-code">
    <ZipCodeContent />
  </SidebarApp>
);

export default ZipCodePage;
