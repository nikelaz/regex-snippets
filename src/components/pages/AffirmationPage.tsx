import SidebarApp from '../SidebarApp';
import AffirmationContent from '../../app/(sidebar-layout)/affirmation/Content';

const AffirmationPage = () => (
  <SidebarApp currentPath="/affirmation">
    <AffirmationContent />
  </SidebarApp>
);

export default AffirmationPage;
