import SidebarApp from '../SidebarApp';
import SSNContent from '../../app/(sidebar-layout)/social-security-number/Content';

const SSNPage = () => (
  <SidebarApp currentPath="/social-security-number">
    <SSNContent />
  </SidebarApp>
);

export default SSNPage;
